"""Custom RL algorithms for unitree_rl_mjlab.

This package hosts algorithm/model/storage extensions that plug into the
installed ``rsl_rl`` via the ``class_name`` resolution mechanism
(``rsl_rl.utils.resolve_callable``), so no installed package is modified.
"""
